/**
 * EchoNotes.
 *
 * One page, three landmark regions, in the order a screen reader user meets them:
 *
 *   1. Library Overview  - the shape of the library, before any detail (Idea11y Section 4.1)
 *   2. Outline           - Subject h1 / Topic h2 / notes as list items (Idea11y DG1, DG2)
 *   3. Assistant         - voice query and answer (EchoNotes Feature 4)
 *
 * Plus a skip link, the live region every announcement flows through, and the global shortcut
 * handler from `docs/accessibility.md`.
 */

export default function App() {
  throw new Error("not implemented");
}
